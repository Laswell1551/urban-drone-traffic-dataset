# processed/  (full dataset — not in git)

This directory is intentionally empty in the repository. Download the full
release and unpack it here so that the `analysis/` scripts can find it:

```
processed/
├── states/        # 30 per-run *.parquet files
├── flights.csv
├── hubs.csv
├── scenarios.csv
└── DATA_DICTIONARY.md
```

Full release: see the repository URL / DOI in the top-level README.
For a quick look without downloading, use `sample/` and `examples/quickstart.py`.
